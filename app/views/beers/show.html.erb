<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @beer.name %>
</p>

<p>
  <strong>Style:</strong>
  <%= @beer.style %>
</p>

<p>
  <strong>Brewery:</strong>
  	<% brewery = Brewery.find_by :id=>@beer.brewery_id %>
    <%= link_to brewery.name, brewery %>
<br />
<% if @beer.ratings.empty? %>
 beer has not yet been rated! <%= link_to "add a rating?", new_rating_path %>

<% else %>
<% c = @beer.ratings.count%>
 Has <%= c %> <%= "rating".pluralize(c) %>, average <%= @beer.average_rating %>
<% end %>  	
  
</p>

<%= link_to 'Edit', edit_beer_path(@beer) %> |
<%= link_to 'Back', beers_path %>
